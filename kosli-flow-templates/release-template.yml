version: 1
trail:
    attestations:
        - name: jira-issues
          type: custom:jira-issues-list
    artifacts:
        - name: backend
          attestations:
            - name: release-approval
              type: custom:approval-jira
